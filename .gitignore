target/
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
pom.xml.next
release.properties
dependency-reduced-pom.xml
buildNumber.properties
.mvn/timing.properties

# Aktuell: Apache Maven Wrapper „only-script“ (keine maven-wrapper.jar im Repo).
# Die Ausnahme hilft, falls später wieder eine Wrapper-JAR eingecheckt oder global *.jar ignoriert wird:
!/.mvn/wrapper/maven-wrapper.jar

.classpath
.project
.settings
settings.json

struktogrammeditor.properties
visustruct.properties

.DS_Store

# Lokale Build- und Verteilungsordner
dist/

# Abhängigkeitsordner irrtümlich im Projektroot (z. B. entpackte JARs)
com/

# Lokaler Klon des SwiftUI-Repos (nicht ins Java-Projekt einchecken)
VisuStruct-swift/